import { Scraped } from '../../scraped'
import { EstridiGeneratorOptions } from '../../index'

export const generateVitest = async (name: string, scraped: Scraped, options: EstridiGeneratorOptions) => {
  return ``
}

